project:
  name: "ml-notebook-project"
  random_seed: 42

paths:
  raw_data: "data/raw"
  interim_data: "data/interim"
  processed_data: "data/processed"
  models: "models"
  figures: "reports/figures"

training:
  target_column: "target"
  test_size: 0.2
  random_state: 42
